from setuptools import setup

setup(name='mmuda_distribution',
      version='0.1',
      description='Gaussian distributions',
      packages=['mmuda_distribution'],
      author='Maximilian Müller',
      zip_safe=False)
